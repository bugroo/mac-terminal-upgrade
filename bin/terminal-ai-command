#!/bin/zsh
# Compatibility guard for tabs that still have the previous command-only widget loaded.
print -u2 -- "Inline AI has been upgraded to conversation. Open a new tab, then type # followed by a question."
exit 2
