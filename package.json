{
  "name": "mac-terminal-upgrade",
  "version": "0.2.0",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@11.5.2",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test tests/*.test.mjs && zsh tests/test-installer.zsh && expect tests/test-ai-zle.exp",
    "check": "node --check bin/terminal-ai-chat.mjs && zsh -n install.sh uninstall.sh update.sh doctor.zsh config/zsh/terminal-upgrade.zsh config/zsh/terminal-ai.zsh bin/terminal-ai-command",
    "test:ai": "node --test tests/*.test.mjs"
  }
}
